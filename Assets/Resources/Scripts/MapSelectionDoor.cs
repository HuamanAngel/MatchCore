using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapSelectionDoor : MonoBehaviour
{
    // Start is called before the first frame update
    public int numberMap;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
